<Window x:Class="WiFiShow.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WiFiShow.WPF"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Wi-Fi Show" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="Transparent"
        FontFamily="{DynamicResource MaterialDesignFont}"
        WindowStyle="None"
        AllowsTransparency="True">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" GlassFrameThickness="0" CornerRadius="16" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <!-- Aurora Background -->
    <Border CornerRadius="16" BorderThickness="0" ClipToBounds="True">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#E0E7FF" Offset="0.0" />
                <GradientStop Color="#BAE6FD" Offset="0.25" />
                <GradientStop Color="#FBCFE8" Offset="0.5" />
                <GradientStop Color="#D1FAE5" Offset="0.75" />
                <GradientStop Color="#FEF08A" Offset="1.0" />
            </LinearGradientBrush>
        </Border.Background>

        <!-- Glass Panel -->
        <Border Margin="24" CornerRadius="16" BorderBrush="#CCFFFFFF" BorderThickness="1.5" Background="#A6FFFFFF">
            <!-- Optional drop shadow for the glass panel -->
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="30" Opacity="0.1" ShadowDepth="4" />
            </Border.Effect>
            
            <materialDesign:DialogHost Identifier="RootDialog" DialogTheme="Inherit">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Custom Title Bar (Transparent) -->
                    <Grid Grid.Row="0" Height="60" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        
                        <!-- Title -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="24,0,0,0">
                            <materialDesign:PackIcon Kind="Wifi" Width="28" Height="28" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}" />
                            <TextBlock Text="Wi-Fi Show" VerticalAlignment="Center" Margin="12,0,0,0" FontWeight="Bold" FontSize="20" Foreground="#0F172A" />
                        </StackPanel>
                        
                        <!-- Actions & Search -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,20,0" WindowChrome.IsHitTestVisibleInChrome="True">
                            <TextBox x:Name="SearchBox" Width="220"
                                     materialDesign:HintAssist.Hint="Search SSID..." 
                                     materialDesign:TextFieldAssist.HasClearButton="True"
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                     Background="#80FFFFFF" BorderBrush="#CCFFFFFF"
                                     TextChanged="SearchBox_TextChanged" Margin="0,0,15,0" />

                            <ToggleButton x:Name="ViewToggleBtn" Style="{StaticResource MaterialDesignActionToggleButton}"
                                          ToolTip="Toggle View (Cards/Table)" Margin="0,0,10,0" Click="ViewToggleBtn_Click"
                                          Background="#80FFFFFF" BorderBrush="#CCFFFFFF">
                                <ToggleButton.Content>
                                    <materialDesign:PackIcon Kind="ViewModule" />
                                </ToggleButton.Content>
                                <materialDesign:ToggleButtonAssist.OnContent>
                                    <materialDesign:PackIcon Kind="Table" />
                                </materialDesign:ToggleButtonAssist.OnContent>
                            </ToggleButton>

                            <Button x:Name="ToggleAllBtn" Style="{StaticResource MaterialDesignOutlinedButton}" 
                                    Margin="0,0,10,0" Click="ToggleAllBtn_Click"
                                    ToolTip="Show/Hide all passwords" Background="#80FFFFFF" BorderBrush="#CCFFFFFF">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Eye" VerticalAlignment="Center" Margin="0,0,5,0" />
                                    <TextBlock Text="Show All" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                            
                            <Button Style="{StaticResource MaterialDesignOutlinedButton}" 
                                    Margin="0,0,10,0" Click="ExportBtn_Click"
                                    ToolTip="Export as CSV" Background="#80FFFFFF" BorderBrush="#CCFFFFFF">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Export" VerticalAlignment="Center" Margin="0,0,5,0" />
                                    <TextBlock Text="Export" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                            
                            <Button Style="{StaticResource MaterialDesignOutlinedButton}" 
                                    Click="RefreshBtn_Click"
                                    ToolTip="Refresh networks" Background="#80FFFFFF" BorderBrush="#CCFFFFFF">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Refresh" VerticalAlignment="Center" Margin="0,0,5,0" />
                                    <TextBlock Text="Refresh" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </StackPanel>

                        <!-- Window Controls -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="True" Margin="0,0,10,0">
                            <Button Style="{StaticResource MaterialDesignFlatButton}" Width="40" Height="40" Click="Minimize_Click" Content="—" Foreground="#64748B" />
                            <Button Style="{StaticResource MaterialDesignFlatButton}" Width="40" Height="40" Click="Maximize_Click" Content="⬜" Foreground="#64748B" />
                            <Button Style="{StaticResource MaterialDesignFlatButton}" Width="40" Height="40" Click="Close_Click" Content="✕" Foreground="#EF4444" />
                        </StackPanel>
                    </Grid>

                    <Separator Grid.Row="1" Margin="24,0" Background="#33000000" />

                    <!-- Main Content Area -->
                    <Grid Grid.Row="2" Margin="24,15,24,24">
                        <ProgressBar x:Name="LoadingSpinner" Style="{StaticResource MaterialDesignCircularProgressBar}" 
                                     Value="0" IsIndeterminate="True" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                     Width="50" Height="50" Visibility="Collapsed" Panel.ZIndex="1" Foreground="{DynamicResource PrimaryHueMidBrush}" />

                        <!-- Cards View -->
                        <ScrollViewer x:Name="CardsView" VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="CardsList">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <!-- Translucent Glass Cards -->
                                        <materialDesign:Card Width="300" Margin="10" Padding="15" 
                                                             Background="#B3FFFFFF" 
                                                             materialDesign:ElevationAssist.Elevation="Dp2">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>
                                                
                                                <TextBlock Grid.Row="0" Text="{Binding Ssid}" FontSize="18" FontWeight="Bold" Foreground="#0F172A" TextTrimming="CharacterEllipsis" />
                                                
                                                <Border Grid.Row="1" Background="#66FFFFFF" CornerRadius="8" Padding="10,5" Margin="0,12,0,0" BorderBrush="#40FFFFFF" BorderThickness="1">
                                                    <StackPanel Orientation="Horizontal">
                                                        <materialDesign:PackIcon Kind="Key" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="#64748B" />
                                                        <TextBox Text="{Binding Password, Mode=OneWay}" IsReadOnly="True" BorderThickness="0" Width="200" VerticalAlignment="Center" Foreground="#64748B" FontFamily="Consolas" Background="Transparent" />
                                                    </StackPanel>
                                                </Border>

                                                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,15,0,0">
                                                    <materialDesign:PackIcon Kind="Security" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="#64748B" />
                                                    <TextBlock Text="{Binding AuthType}" VerticalAlignment="Center" Foreground="#64748B" />
                                                </StackPanel>
                                                
                                                <Grid Grid.Row="3" Margin="0,15,0,0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    
                                                    <StackPanel Orientation="Horizontal" Grid.Column="0">
                                                        <TextBlock Text="Auto-Connect" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#0F172A" />
                                                        <ToggleButton IsChecked="{Binding IsAutoConnect, Mode=OneWay}" 
                                                                      Tag="{Binding Name}"
                                                                      Click="AutoConnectToggle_Click" />
                                                    </StackPanel>

                                                    <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right">
                                                        <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Show QR / Details" Tag="{Binding Name}" Click="DetailsBtn_Click" Foreground="#64748B">
                                                            <materialDesign:PackIcon Kind="InformationOutline" />
                                                        </Button>
                                                        <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Forget Network" Tag="{Binding Name}" Click="DeleteBtn_Click" Foreground="#EF4444">
                                                            <materialDesign:PackIcon Kind="DeleteOutline" />
                                                        </Button>
                                                    </StackPanel>
                                                </Grid>
                                            </Grid>
                                        </materialDesign:Card>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                        <!-- Table View -->
                        <Border x:Name="TableViewContainer" Visibility="Collapsed" Background="#B3FFFFFF" CornerRadius="12" BorderBrush="#CCFFFFFF" BorderThickness="1" Padding="15">
                            <DataGrid x:Name="TableView" AutoGenerateColumns="False" CanUserAddRows="False" 
                                      HeadersVisibility="All" Background="Transparent" BorderThickness="0"
                                      RowBackground="Transparent" AlternatingRowBackground="#1AFFFFFF"
                                      materialDesign:DataGridAssist.CellPadding="13 8 8 8" 
                                      materialDesign:DataGridAssist.ColumnHeaderPadding="8">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Binding="{Binding Ssid, Mode=OneWay}" Header="SSID" Width="2*" IsReadOnly="True" />
                                    <DataGridTextColumn Binding="{Binding Password, Mode=OneWay}" Header="Password" Width="2*" IsReadOnly="True" />
                                    <DataGridTextColumn Binding="{Binding AuthType, Mode=OneWay}" Header="Auth Type" Width="*" IsReadOnly="True" />
                                    <DataGridCheckBoxColumn Binding="{Binding IsAutoConnect}" Header="Auto-Connect" Width="*" IsReadOnly="True" />
                                    <DataGridTemplateColumn Header="Actions" Width="120">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <Button Style="{StaticResource MaterialDesignIconButton}" Width="32" Height="32" ToolTip="Show QR / Details" Tag="{Binding Name}" Click="DetailsBtn_Click" Foreground="#64748B">
                                                        <materialDesign:PackIcon Kind="InformationOutline" />
                                                    </Button>
                                                    <Button Style="{StaticResource MaterialDesignIconButton}" Width="32" Height="32" ToolTip="Forget Network" Tag="{Binding Name}" Click="DeleteBtn_Click" Foreground="#EF4444">
                                                        <materialDesign:PackIcon Kind="DeleteOutline" />
                                                    </Button>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>

                    </Grid>
                </Grid>
            </materialDesign:DialogHost>
        </Border>
    </Border>
</Window>
